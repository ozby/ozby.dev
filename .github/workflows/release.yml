name: Release

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    uses: webpresso/github-actions/.github/workflows/changesets-release.yml@d4fbb7b4449ff74d349aec7506eac8d84d3fea25
    with:
      install_command: pnpm install --frozen-lockfile
      version_command: pnpm run version
      publish_command: pnpm run release:publish
      runner: ubuntu-latest

  deploy:
    needs: release
    if: ${{ needs.release.outputs.should_deploy == 'true' }}
    uses: webpresso/github-actions/.github/workflows/cloudflare-production.yml@d4fbb7b4449ff74d349aec7506eac8d84d3fea25
    with:
      install_command: pnpm install --frozen-lockfile
      verify_command: |
        pnpm run verify:secrets
        pnpm run lint
        pnpm run typecheck
        pnpm run test
        pnpm run verify:deploy-contract
      deploy_command: bun infra/src/deploy/deploy-production.ts --skip-smoke
      smoke_command: >
        node --input-type=module --eval "const urls=['https://ozby.dev/health','https://ozby.dev/'];
        const wait=async(url,attempts)=>{for(let i=0;i<attempts;i+=1){try{const res=await fetch(url,{headers:{'user-agent':'ozby-dev prod smoke'}}); if(res.ok) return;}catch{} await new Promise((r)=>setTimeout(r,5000));} throw new Error('Timed out waiting for '+url);};
        for (const url of urls) await wait(url, url.endsWith('/health') ? 24 : 12);"
      release_version: ${{ needs.release.outputs.release_version }}
      runner: ubuntu-latest
    secrets:
      ci_secret_provider_token: ${{ secrets.CI_SECRET_PROVIDER_TOKEN }}
