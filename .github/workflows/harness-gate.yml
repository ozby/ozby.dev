name: Harness Selection Gate (planned-only)

on:
  pull_request:
    paths:
      - "catalog/agent/harness-surfaces.yaml"
      - "catalog/agent/skills/**"
      - "agent-rules/**"
      - "AGENTS.md"
      - ".husky/**"
      - ".github/actions/**"
      - ".github/workflows/**"
      - "infra/**"
      - "package.json"
      - "test/**"
      - "scripts/**"
      - "apps/client/src/**"
      - "apps/workers/**"

permissions:
  contents: read
  packages: read

env:
  NODE_AUTH_TOKEN: ${{ github.token }}

jobs:
  verdict:
    name: Planned-only harness selection verdict
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: voidzero-dev/setup-vp@13e7afb99c66525824db54e107d667216e795d37 # v1.14.0
        with:
          node-version: "24.16.0"
          cache: true
          run-install: false
      - uses: webpresso/github-actions/.github/actions/setup-wp@2003ca953f336efb779165f748e1ad8408565428
        with:
          version: "0.0.6"
          package-root: true
      - name: Check workflow action pins
        run: node scripts/check-workflow-action-pins.ts
      - run: vp install --frozen-lockfile --ignore-scripts
      - name: Run wp audit guardrails
        continue-on-error: true
        run: wp audit guardrails
      - name: Produce planned-only harness selection verdict
        run: vp exec vitest run --config vitest.config.ts test/ci-governance-contract.test.ts
