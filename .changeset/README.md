# Changesets

This repo uses Changesets for release versioning.

- Run `vp run changeset`
- Commit the generated `.changeset/*.md`
- Merge to `main` to let CI open/update the **Version Packages** PR

Do not manually push release tags.
